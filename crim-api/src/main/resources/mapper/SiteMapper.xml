<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heycm.mapper.SiteMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.heycm.model.Site">
        <id column="id" property="id" />
    <result column="create_user" property="createUser" />
    <result column="create_time" property="createTime" />
    <result column="modify_user" property="modifyUser" />
    <result column="modify_time" property="modifyTime" />
    <result column="is_deleted" property="isDeleted" />
        <result column="building_id" property="buildingId" />
        <result column="site_name" property="siteName" />
        <result column="site_desc" property="siteDesc" />
        <result column="status" property="status" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        create_user,
        create_time,
        modify_user,
        modify_time,
        is_deleted,
        id, building_id, site_name, site_desc, status
    </sql>

<!--    分页查询场地信息 -->
    <select id="pageList" resultType="com.heycm.dto.SideDTO">
        SELECT t1.* FROM (
            SELECT ct.id campus_id, ct.type_name campus_name, cb.id building_id, cb.building_name, cs.id site_id, cs.site_name, cs.site_desc FROM crim_type ct
            INNER JOIN crim_building cb ON ct.id = cb.campus_id
            INNER JOIN crim_site cs ON cb.id = cs.building_id
            WHERE ct.pid = 1 AND ct.is_deleted = 0 AND cb.is_deleted = 0 AND cs.is_deleted = 0
        ) t1
        ${ew.customSqlSegment}
    </select>

</mapper>
